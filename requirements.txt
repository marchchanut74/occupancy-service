# Occupancy Detection Service — Dependencies (CPU-only)
# ต้องใช้ Python 3.10 ขึ้นไป
#
# ใช้ PyTorch CPU-only → ประหยัด disk ~1.5GB
# ถ้าต้องการ GPU ลบบรรทัด --extra-index-url ออก

--extra-index-url https://download.pytorch.org/whl/cpu

# ── Core ──
ultralytics>=8.0
opencv-python-headless>=4.8
numpy>=1.24

# ── API Server ──
fastapi>=0.100
uvicorn[standard]>=0.22
python-multipart>=0.0.6        # สำหรับ file upload

# ── Config ──
pyyaml>=6.0

# ── MQTT (optional — ถ้าไม่ใช้ MQTT ลบบรรทัดนี้ได้) ──
paho-mqtt>=1.6,<2.0
